## 优化

- 兼容 Google OpenAI 兼容模式下 Uni API 的图片模型别名与默认模型映射，减少代理配置不一致导致的请求失败

## 修复

- 有参考图时优先走 `/v1/images/edits`，无参考图时继续走 `/v1/images/generations`，修正图生图请求路径
- 当代理返回 `channel not implemented` 时自动从编辑通道降级到生成通道，并附带参考图数据重试，提高兼容性